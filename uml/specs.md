# 📑 SPECS.md: Спецификация модулей TaskManager

## Модуль: `data.py` (Хранение и загрузка задач)

| Функция | Вход | Выход | Описание |
|:---|:---|:---|:---|
| `load_tasks()` | None | `List[dict]` | Читает данные из `tasks.json`. Если файл не найден или невалиден, возвращает пустой список `[]`. |
| `save_tasks(tasks)` | `List[dict]` | `None` | Записывает текущий список задач в файл `tasks.json` с форматированием (`indent=4`). |

## Модуль: `ui.py` (Пользовательский интерфейс)

| Функция | Вход | Выход | Описание |
|:---|:---|:---|:---|
| `show_menu()` | None | `str` | Выводит главное меню. Запрашивает ввод у пользователя и возвращает выбранный пункт (например, '1', '2', 'q'). |
| `get_task_input()` | None | `dict` | Запрашивает у пользователя название и описание новой задачи. Возвращает словарь вида `{'title': ..., 'description': ...}`. |
| `display_tasks(tasks)` | `List[dict]` | `None` | Красиво форматирует и выводит список всех задач в консоль. |
| `get_task_id()` | None | `int` | Запрашивает ID задачи для операций (изменение, удаление, редактирование). Обрабатывает неверный ввод. |
| `get_new_status()` | None | `str` | Запрашивает новый статус задачи (`новая`, `в работе`, `завершена`). Возвращает валидный статус. |

## Модуль: `logic.py` (Бизнес-логика)

| Функция | Вход | Выход | Описание |
|:---|:---|:---|:---|
| `get_next_id(tasks)` | `List[dict]` | `int` | Определяет следующий уникальный ID для новой задачи (макс. ID + 1). Если список пуст, возвращает 1. |
| `add_task(tasks, task_data)` | `List[dict]`, `dict` | `None` | Добавляет задачу в список. Присваивает новый ID и статус `"новая"`. |
| `change_status(tasks, task_id, new_status)` | `List[dict]`, `int`, `str` | `bool` | Находит задачу по `task_id` и меняет её статус. Возвращает `True` при успехе, `False`, если ID не найден. |
| `edit_task(tasks, task_id, title, description)` | `List[dict]`, `int`, `str`, `str` | `bool` | Обновляет название и/или описание задачи. Возвращает `True` при успехе. |
| `delete_task(tasks, task_id)` | `List[dict]`, `int` | `bool` | Удаляет задачу по `task_id`. Возвращает `True` при успехе. |